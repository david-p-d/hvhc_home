<%= form_with(model: table_submission, local: true) do |form| %>
  <% if table_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table_submission.errors.count, "error") %> prohibited this table_submission from being saved:</h2>

      <ul>
      <% table_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
